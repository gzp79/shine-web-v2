# Shine

Shine web app (Svelte 5 + SvelteKit).

## Getting started

```bash
pnpm i
pnpm run env:mock   # MSW mocks — no backend needed
pnpm run dev
```

To run against the **real local backend** (identity, builder, assets), see
[docs/LOCAL_DEV.md](docs/LOCAL_DEV.md).
